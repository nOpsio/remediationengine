{
  "name": "nops-remedy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "package": "aws cloudformation package --template-file template.yaml --output-template-file output.yaml --s3-bucket nopsremediationwest2",
    "deploy": "aws cloudformation deploy --template-file output.yaml --stack-name nops-remedy --capabilities CAPABILITY_IAM --parameter-overrides EmailID=gagan.a@live.com",
    "push": "npm run package && npm run deploy"
  },
  "author": "Gagan"
}
